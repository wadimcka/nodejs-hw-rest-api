# 1. Встановлюємо середовище
FROM node  
# 2. Створюємо кореневу папку
WORKDIR /app
# 3. Копіюємо файли (вказати звідки та куди) з проекту
COPY . /app
# 4. Встановлюємо залежності
RUN npm install
# 5. Вказуємо порт на якому запустити сервер
EXPOSE 3000
# 6. Створюємо команду запуску проекту
CMD [ "npm", "start" ]
# 7. Запускаємо збирання образу командою у терміналі
# docker build .


